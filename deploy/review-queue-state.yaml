apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: review-queue-state
  namespace: bluefin-system
  labels:
    app.kubernetes.io/name: review-queue
    app.kubernetes.io/component: dashboard-state
    app.kubernetes.io/part-of: review
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
